## HTTP port for backend server
PORT=3000
HOST=0.0.0.0

## JWT secret for signing auth tokens (change this in production)
JWT_SECRET=changeme-in-dev

## MySQL connection string
# Examples:
# - Local MySQL with password:  mysql://USER:PASSWORD@localhost:3306/drone_fastfood
# - Local MySQL (root, no password): mysql://root:@localhost:3306/drone_fastfood
# - Docker (docker-compose.yml in backend): mysql://root:1234@localhost:3306/drone_fastfood
DATABASE_URL=mysql://root:1234@localhost:3306/drone_fastfood

## Secret to verify payment webhooks in mock provider
PAYMENT_WEBHOOK_SECRET=dev-webhook-secret

## CORS nhiều máy: danh sách các origin frontend được phép truy cập (phân tách bằng dấu phẩy)
# Ví dụ: CORS_ORIGINS=http://10.61.71.220:5173,http://26.181.61.232:5173
CORS_ORIGINS=http://localhost:5173

## Rate limit cơ bản (ms & số requests tối đa / window). Mặc định 60000ms và 120.
RATE_LIMIT_WINDOW=60000
RATE_LIMIT_MAX=120

## Token truy cập /api/metrics (optional). Nếu để trống sẽ mở public.
METRICS_TOKEN=changeme-metrics

# OSRM/ORS routing config (miễn phí)
# Dùng OSRM demo cho dev (khuyên tự host khi production)
# OSRM_BASE_URL=http://localhost:5000
# Hoặc dùng public demo (chỉ dev):
# OSRM_BASE_URL=https://router.project-osrm.org
# Nếu chọn OpenRouteService (cần API key free tier):
# ORS_API_KEY=replace-with-your-ors-key
